import { SortUsernamePipe } from './sort-username.pipe';

describe('SortUsernamePipe', () => {
  it('create an instance', () => {
    const pipe = new SortUsernamePipe();
    expect(pipe).toBeTruthy();
  });
});
