import { ShowCategoryPipe } from './show-category.pipe';

describe('ShowCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
